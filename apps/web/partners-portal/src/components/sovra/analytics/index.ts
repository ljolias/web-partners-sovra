/**
 * Analytics Components
 *
 * This module exports all analytics-related UI components for the
 * Training Center analytics dashboard.
 *
 * Components:
 * - OverviewMetrics: Displays 4 key metric cards (courses, enrollments, completion, certifications)
 * - CredentialStats: Displays credential claim metrics (issued, claim rate, claim time)
 * - EnrollmentChart: Line chart showing enrollment trends over time
 * - CompletionChart: Line chart showing completion trends over time
 * - CoursePerformanceTable: Sortable table showing course-level metrics
 * - DateRangeFilter: Date range filter with presets and custom picker
 */

export { OverviewMetrics } from './OverviewMetrics';
export { CredentialStats } from './CredentialStats';
export { EnrollmentChart } from './EnrollmentChart';
export { CompletionChart } from './CompletionChart';
export { CoursePerformanceTable } from './CoursePerformanceTable';
export { DateRangeFilter } from './DateRangeFilter';
